# Faraday-cage etching

Charge-driven faraday-cage effects focus the etch front.

## faradayCageEtching

Run from the repository root:

```bash
cd docs/examples/faradayCageEtching && uv run python faradayCageEtching.py -D 2
```

```python
--8<-- "examples/faradayCageEtching/faradayCageEtching.py"
```
